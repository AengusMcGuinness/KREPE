bits = 65536
ini = 1
myArray = makeBitArray(bits, ini)
bit = 0
clearBit(myArray, bit)
bit = 1
clearBit(myArray, bit)

for index in range(256):
    test = testBit(myArray, index)
##the part here is where i think we'd adapt for our needs (below)
    if test:
        zeroBit = index * index
        while zeroBit <65536:
            clearBit(myArray, zeroBit)
            zeroBit += index

for index in range(655
